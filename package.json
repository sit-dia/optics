{
  "name": "optics",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "postbuild": "echo optics.diaversity.org > docs/CNAME",
    "preview": "vite preview",
    "test": "vitest --reporter=verbose",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vitest": "^2.1.4"
  }
}
